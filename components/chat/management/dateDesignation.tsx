import styled from '@emotion/styled';
import { NextPage } from 'next';

const DateDesignation: NextPage = () => {
  return <DateDesignationSection></DateDesignationSection>;
};

const DateDesignationSection = styled.section`
  width: 25vw;
  height: 100%;
  background-color: white;
`;

export default DateDesignation;
